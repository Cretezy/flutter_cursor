name: flutter_cursor
description: Custom cursor support for Flutter (for hovering on web) with widgets
version: 0.1.1
homepage: https://github.com/Cretezy/flutter_cursor

environment:
  sdk: ">=2.0.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  universal_html: ^1.2.0
